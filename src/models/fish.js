export default class Fish {
    constructor(name, price) {
      this.name = name;
      this.count = 1;
      this.price = this.price;
    }
  
    incrementCount() {
      this.count += 1;  
    }
  }
  